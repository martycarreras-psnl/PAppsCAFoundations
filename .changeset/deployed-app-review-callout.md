---
"@pacaf/wizard": patch
---

The deployed Code App's landing page now shows a "fresh-eyes review" callout directly under the hero, above the two path cards. It recommends the opinionated workflow of starting a brand-new agent session to review the initial implementation against the plan (CONTEXT.md, dataverse/planning-payload.json, docs/adr) and the project's rules before functional testing, and includes a copyable handoff prompt for that review session.
